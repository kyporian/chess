public class Validator {
    //distributor for different pieces
    public static boolean validMove(char piece) {
        if (piece == 'k' || piece == 'K') {
            return true; //validKing(piece);
        }
        
        return true;
    }

    


}
